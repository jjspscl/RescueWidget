from argparse import ArgumentParser
from RescueWidget import *

def main(): 
    # argp = ArgumentParser(description=APP_DESC)
    #options = Options()
    tray = widget.TrayIcon()
    widget.Gtk.main()
	
if __name__ == "__main__":
    main()
